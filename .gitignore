# dépendances
/node_modules

# build Next.js
/.next
/out

# variables d'environnement (SECRÈTES — ne jamais commiter)
# Ignore TOUS les .env (y compris .env.production / .env.development),
# sauf le template d'exemple volontairement versionné.
.env
.env.*
!.env.local.example

# infos de build TypeScript
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# bot Discord (projet séparé, non déployé avec le site)
/discord-bot

# outil de captures (dev only)
/screencomp

# divers
.DS_Store
*.pem
.vercel
next-env.d.ts
